services:
  api:
    build: .
    container_name: employee-search-api
    environment:
      - APP_DB_PATH=/app/data/app.db
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
